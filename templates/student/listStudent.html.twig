<a href="{{path('app_addStudent')}}">Add Student</a>
<h4>List Students</h4>
{% for i in tabStudents %}
    id:{{ i.nce }}
    name:{{ i.username  }}
    classe: {{ i.Classe }}
    <a href="{{ path('app_updateStudent',{'nce':i.nce}) }}">Update</a>
    <a href="{{ path('app_removeStudent',{'nce':i.nce}) }}">Delete</a>
    <br>
{% endfor %}
